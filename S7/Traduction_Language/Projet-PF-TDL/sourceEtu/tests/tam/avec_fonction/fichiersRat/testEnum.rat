enum Jour {Lundi, Mardi};

int estMardi(Jour j) {
  if (j = Mardi) {
    return 99;
  } else {
    return 0;
  }
}

test {
  Jour j = Lundi;
  print Mardi;        // Doit afficher 1
  print estMardi(Mardi); // Doit afficher 99
}